#include <stdio.h>
#include <stdlib.h>
#include <math.h> // biblioteca de matemática

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
int soma=0,
	aoba=1;
void main(){
	 // %d  inteiro
	 //%f   float
	 //%c	caracter
	 //%s	string
	 //%%	 coloca % na tela 
	 //sleep(1)  == delay(1000)
	 //& ponteiro endereço da memória
	 // declarar ponteiro
	 //int *ptr 
	 /*
	 ptr = &aoba // o ponteiro recebeu o endereço de memoria da variavel aoba
	 
	 */
	printf("STM32 Curso: %d e o aoba...%d",soma,aoba);
}// end man
